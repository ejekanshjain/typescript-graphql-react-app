mutation SignIn($email: String!, $password: String!) {
  signIn(data: { email: $email, password: $password }) {
    userId
    role
    email
    firstName
    lastName
    profileImage
    accessToken
    issuedAt
    expiresAt
  }
}
